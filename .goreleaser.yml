builds:
  - binary: mackerel-plugin-sslhandshake-time
    goos:
      - linux
    goarch:
      - amd64
      - arm64
archives:
  - format: zip
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
release:
  github:
    owner: ryuichi1208
    name: mackerel-plugin-sslhandshake-time
